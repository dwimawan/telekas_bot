{
  "name": "tele-finance-bot",
  "version": "1.0.0",
  "description": "Bot Telegram pencatatan keuangan pribadi — Vercel + Google Sheets",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "deploy": "vercel --prod",
    "check-webhook": "node scripts/check-webhook.js",
    "set-webhook": "node scripts/set-webhook.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  },
  "notes": {
    "zero-dependencies": "Sengaja tanpa library eksternal. Auth Google Sheets pakai JWT manual (Node.js built-in crypto). Fetch API pakai Node 18 native fetch."
  }
}
